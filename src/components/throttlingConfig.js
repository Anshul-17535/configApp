import React,{Component} from 'react'
import Index from '../containers/throttlingConfig/index';

class ThrottlingConfig extends Component{
    render(){
        return(
            <Index/>
        )
    }
}

export default ThrottlingConfig